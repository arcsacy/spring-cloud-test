server:
  port: 8888
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: gateway
      profile: dev
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/