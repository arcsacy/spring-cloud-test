server:
  port: 9001
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: auth
      profile: dev
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/