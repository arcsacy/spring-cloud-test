server:
  port: 9002
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: business
      profile: dev
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/